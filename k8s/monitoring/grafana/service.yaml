apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring
spec:
  type: ClusterIP    # Access via kubectl port-forward or Ingress
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
